<%= simple_form_for @page do |f| %>
  <%= f.input :title %>
  <%= f.input :key %>
  <%= f.input :body, input_html: {class: 'simditor'} %>
  <div class="actions">
    <%= f.submit 'Save', class: 'btn btn-primary' %>
    <%= link_to 'Back', pages_path, class: 'btn btn-default' %>
  </div>
<% end %>